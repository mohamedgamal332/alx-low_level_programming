#include <stdio.h>

#define NEWLINE '\n'

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int c;

	for (c = 0; c <= 9; ++c)
	{
		f = (char) c;
		putchar(f);
	}
	putchar(NEWLINE)
	return (0);
}
